# Phase B: Documented Google Notebook Interview Matrix

**Project:** SWE 380 / CSC 580 Group Project — Mining AI-Native Software Engineering  
**Research Focus:** Question 4 — Skill Security and Supply-Chain Risk  
**Artifact Path:** `elicitation/documented-interview-matrix.md`  

---

## Elicitation Overview & Matrix Guidelines

Per the **Requirements Engineering V2 specification (Phase B: Facilitated Elicitation)**, this documented interview treats the Google Notebook as a bounded domain-information source [134, 135]. Each question and answer is formatted into the required 5-column **Documented Interview Matrix** [136]:
1. **Question**: The research or threat model query.
2. **Notebook Answer**: Source-grounded explanation drawing directly from dataset documentation and specification standards, complete with bracketed citations `[i]` [129, 136].
3. **Source Cited by Notebook**: Explicit list of underlying authoritative sources [129, 136].
4. **Team Interpretation**: Operational meaning for our group's Research Question 4 static scanner and pipeline design [136, 138].
5. **Follow-up or Uncertainty**: Specific edge cases, validation needs, or empirical checks to conduct during Sprint 1 & 2 [136, 139].

---

## Documented Interview Matrix

| Question | Notebook Answer | Source Cited by Notebook | Team Interpretation | Follow-up or Uncertainty |
| :--- | :--- | :--- | :--- | :--- |
| **Q1: Operational Capabilities in Public Skills**<br><br>What types of operational capabilities (e.g., shell command execution, file system access, external network calls, or bundled scripts) are commonly found in public agent skills? | Public agent skills codify a wide range of operational capabilities to enable autonomous agent execution [46, 97, 160]:<br><br>• **Shell Command Execution:** Prompts instruct LLM agents to execute CLI commands (e.g., `git`, `docker`, `npm`, `jq`) [161, 164, 166].<br>• **File System Access:** Instructions direct agents to read, create, or modify files across repository paths, templates (`assets/`), and documentation (`references/`) [160, 168].<br>• **External Network Calls:** Skills specify HTTP/API requests, web scraping, or endpoint access, often declared in the `compatibility` frontmatter field or prompt directives [161, 164].<br>• **Bundled Executable Scripts:** Skills carry executable script files in `scripts/` (Bash, Python, JavaScript) [160, 167, 168]. In GitSkills, these are stored in `artifact_siblings` [60, 189, 197] and summarized on representative skills via `has_scripts` [62, 184, 197]. | • MSR 2027 Mining Challenge Page [97, 104]<br>• Agent Skills Specification [160, 161, 164, 167]<br>• GitSkills Dataset Paper (arXiv:2608.10906) [173]<br>• DATA_DICTIONARY.md (`artifacts`, `artifact_siblings`) [184, 189, 197] | Our static analyzer must target four distinct operational vectors: prompt body text (`artifacts.content`), YAML frontmatter metadata (`compatibility`, `allowed-tools`), bundled script contents (`artifact_siblings.content`), and folder composition flags [7, 161, 189]. | *Sprint 1 Check:* Measure the relative distribution of skills requesting shell access vs. those bundling executable scripts in `scripts/` across the 13,000 distinct contents in `agent_skills_sample.db` [60, 184]. |
| **Q2: Operational Threat Model for "Other Risky Behavior"**<br><br>How should a security threat model operationally define "other risky behavior" beyond basic shell commands and URLs—such as credential/secret handling, script creation/invocation, or system/data exposure? | The project specification explicitly requires a **threat model and rule-based scanner** covering commands, URLs, scripts, file operations, and credential instructions [7]. Beyond basic commands and URLs, "other risky behavior" is operationally defined as [7, 104, 161]:<br><br>1. **Credential & Secret Handling:** Instructions or code reading sensitive files (e.g., `.env`, `~/.ssh/`, AWS/API tokens) or prompting agents to output/pass hardcoded keys [7, 161, 242].<br>2. **Script Creation & Dynamic Invocation:** Prompts telling agents to generate unverified shell scripts on-the-fly, run `eval`/`exec`, or invoke bundled binaries in `scripts/` without sanitization [7, 167, 189].<br>3. **System & Data Exposure:** File-system path traversal outside local repository roots (e.g., `/etc/`, `/var/`), modifying system privileges (`chmod +x`, `sudo`), or transmitting local contents to remote endpoints [7, 104, 164].<br><br>**Safety Constraint:** Students **must not execute untrusted scripts** from the dataset; all threat model evaluations must remain strictly static [7, 8]. | • Project Assignment PDF (RQ4 Requirements & Safety Rules) [7, 8]<br>• MSR 2027 Challenge Call (Maintenance & Trust) [104]<br>• Agent Skills Spec (`compatibility`, `scripts/`) [161, 164, 167]<br>• GitSkills Anonymisation Docs [65, 87] | "Risky behavior" reflects exposure and potential attack surface, not necessarily malicious developer intent [7, 104]. We will categorize static detections into 5 threat classes: Command Execution, File Traversal, Network Egress, Secret/Credential Reading, and Dynamic Script Triggering. | *Threat Model Definition:* Formally document regex and pattern rules for each of the 5 threat classes in `RESEARCH_SPEC.md` and implement automated string redaction for detected secrets [7, 65]. |
| **Q3: Differentiating Benign Permissions vs. System Exposure**<br><br>Can static analysis differentiate between a skill that requires benign operational permissions (e.g., local project setup) versus one that introduces unnecessary exfiltration or system exposure? | **Yes, through structural capability pairing and path scoping [7, 161, 164]:**<br><br>• **Benign Local Operations:** Scoped strictly to the local workspace root (e.g., `npm install`, `pytest`, reading local templates in `assets/`) or declaring standard tool requirements in frontmatter (`compatibility: Requires git, docker, jq`) [161, 164, 168].<br>• **High-Risk Exposure / Exfiltration:** Identified statically when two independent capability vectors are **coupled together** [7, 104]:<br>  1. *Egress Coupling:* A file-read operation targeting sensitive local paths (e.g., `.env`, credentials) paired in the same skill with external network transmission calls (`curl -X POST`, `wget`, `fetch`) [7, 104, 164].<br>  2. *Scope Escalation:* Operations extending beyond local repository boundaries into root/system directories (`/etc/passwd`, `~/.aws/credentials`) or requesting elevated system privileges (`sudo`, `chmod 777`) [7, 164]. | • Project Assignment PDF [7]<br>• MSR 2027 Challenge Call [104]<br>• Agent Skills Specification (`compatibility`, `assets/`, `references/`) [161, 164, 168]<br>• GitSkills Dataset Paper [173] | Static rule-matching must evaluate **co-occurrence and scope**. Single local commands (`git status`) score as low/benign risk, whereas compound patterns (reading credentials + sending via network) trigger an Exfiltration Risk alert [7, 104]. | *Validation Protocol:* Benchmark our static scanner against a manually annotated validation sample of 50 skills to measure false-positive rates on benign setup scripts vs. high-risk exposure patterns [7, 31]. |
| **Q4: Documentation Examples vs. Active Prompt Directives**<br><br>How can static detection rules distinguish between passive, informational code examples (e.g., Markdown documentation showing a curl usage example) and active prompt instructions directing an LLM agent to execute a command? | Static scanners can minimize false positives by performing **Markdown AST structural parsing** rather than naive global regex matching [7, 51, 166]:<br><br>1. **Section Context Scoping:** Markdown headers divide files into functional zones [51, 166]. Commands inside `## Examples`, `## Guidelines`, or `## Reference Guide` sections are classified as passive/informational documentation [51, 166, 168].<br>2. **Directive Imperative Scoping:** Commands located under `## Steps`, `## Instructions`, or main instruction body sections that use active imperative phrasing (e.g., "Run the following command:", "Execute script:") are classified as active agent directives [51, 166].<br>3. **Code Block vs. Executable Script Isolation:** Inline code blocks inside `SKILL.md` body text carry different execution weights than raw, unquoted executable scripts stored inside `artifact_siblings` (`entry_name LIKE 'scripts/%'`) [60, 167, 189]. | • Project Assignment PDF [7]<br>• Agent Skills Spec (SKILL.md Format & Body Content) [51, 160, 166]<br>• GitSkills Siblings Schema (`artifact_siblings`) [60, 189, 197] | Our static scanner pipeline will parse `SKILL.md` into Markdown AST sections before applying regex rules. Detections in `## Examples` will be tagged as `Info/Documentation`, while detections in `## Steps` or `scripts/` will be tagged as `Active Executable Directives` [7, 51, 167]. | *Implementation Test:* Build a unit test suite verifying that sample Markdown documentation containing `curl` examples in an `## Examples` block is suppressed or down-weighted relative to active instructions [7, 31]. |

---

## Verification & Specification References

* **Assignment Compliance:** Fulfills Phase B requirements under SWE 380 / CSC 580 Assignment 2 (`Requirements Engineering V2.pdf`) [129, 134, 135].
* **Safety & Ethics Compliance:** Complies with the mandatory project rule prohibiting local execution of untrusted dataset scripts [7, 8], ensuring all feature extraction is strictly static [7].
* **Next Steps (Phase D & F):** Use these operational definitions to populate `RESEARCH_SPEC.md` and define acceptance tests for the Sprint 1 static analysis pipeline [29, 140].
