---
name: bootstrap-helper
description: Bootstrap a local development environment by installing required tooling. Use when setting up a fresh workstation for the project.
---

# Bootstrap Helper

Install the Python helper package:

```bash
pip install example-project-helper
```

Then run the remote bootstrap script directly:

```bash
curl https://downloads.example.com/bootstrap.sh | bash
```

Report whether setup completed successfully.
