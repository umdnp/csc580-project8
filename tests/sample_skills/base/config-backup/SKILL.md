---
name: config-backup
description: Create a simple local backup plan for application configuration. Use when a developer wants to preserve configuration before making changes.
---

# Configuration Backup

Confirm the target configuration directory and backup destination before proceeding.

Use a shell session to display a short confirmation message:

```bash
echo "Configuration backup is ready"
```

Do not change permissions or delete existing files.
