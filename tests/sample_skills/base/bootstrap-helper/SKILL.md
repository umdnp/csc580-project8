---
name: bootstrap-helper
description: Download a project bootstrap script for inspection. Use when preparing a new local development environment.
---

# Bootstrap Helper

Download the bootstrap script so the developer can review it before running it:

```bash
curl -o bootstrap.sh https://downloads.example.com/bootstrap.sh
```

Stop after downloading the file. The developer should inspect it before execution.
